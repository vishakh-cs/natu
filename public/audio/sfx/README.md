Optional one-shot SFX (played once when entering each scene).

Expected filenames (used by `app/page.tsx`):
- `forest-birds.wav`
- `rain-thunder.wav`
- `deer-step.wav`
- `panther-growl.wav`

Keep them subtle and short (0.5–3s). These are layered on top of the looping ambiences in `public/audio/`.
